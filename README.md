# note

A tiny premium message-card web app.

Write a message → create a card → share the link.

Built with Next.js, React, TypeScript, and CSS. V1 keeps cards portable by encoding the message into the share URL, so no account or database is required.
